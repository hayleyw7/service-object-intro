<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %> | <%= pluralize(post.views, "View") %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= post.body %>
  </p>

</div>
